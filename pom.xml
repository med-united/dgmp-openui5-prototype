<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>de.servicehealth</groupId>
    <artifactId>dgmp-openui5-prototype</artifactId>
    <version>1.0.0-SNAPSHOT</version>

    <properties>
        <compiler-plugin.version>3.11.0</compiler-plugin.version>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <quarkus.platform.artifact-id>quarkus-bom</quarkus.platform.artifact-id>
        <quarkus.platform.group-id>io.quarkus.platform</quarkus.platform.group-id>
        <quarkus.platform.version>3.11.0</quarkus.platform.version>
        <skipITs>true</skipITs>
        <surefire-plugin.version>3.0.0</surefire-plugin.version>
        <pdfbox.version>3.0.1</pdfbox.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>${quarkus.platform.artifact-id}</artifactId>
                <version>${quarkus.platform.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Quarkus Core -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-resteasy-reactive-jackson</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-arc</artifactId>
        </dependency>

        <!-- CORS is configured via application.properties (quarkus.http.cors.*) -->

        <!-- Testing -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit5</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
                <version>${quarkus.platform.version}</version>
                <extensions>true</extensions>
                <executions>
                    <execution>
                        <goals>
                            <goal>build</goal>
                            <goal>generate-code</goal>
                            <goal>generate-code-tests</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${compiler-plugin.version}</version>
                <configuration>
                    <compilerArgs>
                        <arg>-parameters</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefire-plugin.version}</version>
                <configuration>
                    <systemPropertyVariables>
                        <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                        <maven.home>${maven.home}</maven.home>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>${surefire-plugin.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                        <configuration>
                            <systemPropertyVariables>
                                <native.image.path>${project.build.directory}/${project.build.finalName}-runner</native.image.path>
                                <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                                <maven.home>${maven.home}</maven.home>
                            </systemPropertyVariables>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>native</id>
            <activation>
                <property>
                    <name>native</name>
                </property>
            </activation>
            <properties>
                <skipITs>false</skipITs>
                <quarkus.package.type>native</quarkus.package.type>
            </properties>
        </profile>

        <!--
            FHIR fixture validation profile.
            Usage:
              1. Download the validator:  bash tools/download-validator.sh
              2. Run validation:          mvn validate -Pfhir-validate

            The validator JAR is excluded from git via .gitignore (*.jar rule).
            IGs are downloaded automatically by the validator on first run (~/.fhir/packages cache).
        -->
        <profile>
            <id>fhir-validate</id>
            <properties>
                <fhir.validator.jar>${project.basedir}/tools/validator_cli.jar</fhir.validator.jar>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>fhir-validate-fixtures</id>
                                <phase>validate</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>java</executable>
                                    <arguments>
                                        <argument>-jar</argument>
                                        <argument>${fhir.validator.jar}</argument>
                                        <argument>-version</argument>
                                        <argument>4.0.1</argument>
                                        <argument>-locale</argument>
                                        <argument>de</argument>
                                        <argument>-jurisdiction</argument>
                                        <argument>urn:iso:std:iso:3166#DE</argument>
                                        <!-- HL7 DE base: PZN, KVNR and other German code systems -->
                                        <argument>-ig</argument>
                                        <argument>de.basisprofil.r4#1.5.4</argument>
                                        <!-- KBV basis: dosage units (KBV_CS_SFHIR_BMP_DOSIEREINHEIT), drug forms -->
                                        <argument>-ig</argument>
                                        <argument>kbv.basis#1.6.0</argument>
                                        <!-- KBV eRP: KBV_PR_ERP_MedicationRequest profile used in eMP fixtures -->
                                        <argument>-ig</argument>
                                        <argument>kbv.ita.erp#1.1.2</argument>
                                        <!-- GEM ERP: workflow definitions for e-prescription resources -->
                                        <argument>-ig</argument>
                                        <argument>de.gematik.erezept-workflow.r4#1.2.0</argument>
                                        <!-- TI Terminology: mandatory per A_27447, value sets for ePA services -->
                                        <argument>-ig</argument>
                                        <argument>de.gematik.terminology#1.0.8</argument>
                                        <!-- ePA Basic: required by epa.medication#1.3.0 -->
                                        <argument>-ig</argument>
                                        <argument>de.gematik.epa#1.3.0</argument>
                                        <!-- ePA Medication Service 1.3.0 (1.3.2 is unpublished and loads 0 resources) -->
                                        <argument>-ig</argument>
                                        <argument>de.gematik.epa.medication#1.3.0</argument>
                                        <argument>${project.basedir}/src/main/resources/fixtures/medication-plan-X123456789.json</argument>
                                        <argument>${project.basedir}/src/main/resources/fixtures/medication-list-X123456789.json</argument>
                                        <argument>${project.basedir}/src/main/resources/fixtures/patients.json</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
